name: Pull Request

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    container: oven/bun:1
    steps:
      - uses: actions/checkout@v6
      - run: bun install --frozen-lockfile
      - run: bun test
